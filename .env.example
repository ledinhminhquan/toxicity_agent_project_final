# ============================================================
# Environment variables for Toxicity Agent
# Copy this file to .env and fill in your values.
# ============================================================

# Artifacts directory (models, runs, logs)
# ARTIFACTS_DIR=/path/to/artifacts

# Model directory override
# TOXICITY_MODEL_DIR=/path/to/models

# Run directory override (logs, eval results, etc.)
# TOXICITY_RUN_DIR=/path/to/runs

# Data directory override
# DATA_DIR=/path/to/data

# Inference config path (used by the API)
# TOXICITY_INFER_CONFIG=configs/infer.yaml

# HuggingFace cache (useful for Colab + Drive)
# HF_HOME=/path/to/hf_cache
# HF_DATASETS_CACHE=/path/to/hf_cache/datasets
# TRANSFORMERS_CACHE=/path/to/hf_cache/transformers
