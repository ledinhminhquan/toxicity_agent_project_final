version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Optionally point to mounted model directory
      - TOXICITY_MODEL_DIR=/app/models
    volumes:
      # Mount local model directory into container (optional)
      - ./models:/app/models
